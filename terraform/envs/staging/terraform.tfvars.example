# Copy this file to terraform.tfvars and update the values

# Environment
environment = "staging"
aws_region  = "ca-central-1"

# Application Configuration
# app_image = ""  # Leave empty to use auto-created ECR repository, or specify custom image URI

# Neo4j Password (automatically generated by Terraform)

# Optional: SSL Certificate for HTTPS (leave empty for staging)
certificate_arn = ""

# Optional: Custom domain name (leave empty for staging)
domain_name = ""

# VPC Configuration (optional overrides)
# vpc_cidr = "10.0.0.0/16"
# availability_zones = ["ca-central-1a", "ca-central-1b"]

# Resource Sizing (optional overrides for staging)
# app_cpu = 512
# app_memory = 1024
# desired_count = 1

# neo4j_cpu = 1024
# neo4j_memory = 2048

# opensearch_instance_type = "t3.small.search"
# opensearch_instance_count = 1
# opensearch_volume_size = 10 